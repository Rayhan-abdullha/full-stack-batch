let message: string = 'Be a humble man'
message = 'Hi, Rayhan'

const age: number = 28
let numbers: number[] = [1, 8, 9]
let person: string[] = []
person.push('rayhan')
numbers.push(10)
const flags: boolean = true
// console.log(person)

enum Role {
    ADMIN = 'ADMIN',
    MODERATOR = 'MODERATOR',
    USER = 'USER',
    MANAGER = "MANAGER"
}
// console.log(Role.ADMIN)

enum status {
    ACTIVE = 'ACTIVE',
    INACTIVE = "INACTIVE"
}
// console.log(status)

